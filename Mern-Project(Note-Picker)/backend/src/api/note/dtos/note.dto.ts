export interface notePramas {
    noteId: string;
  }
  
export interface updateBody {
    title?: string;
    text?: string;
  }

export interface CreateNotebody {
    title: string;
    text?: string;
  }
